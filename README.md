
# mvn_install Step
Install dependencies use `mvn install`

### INPUTS

## EXAMPLE 

```yml
steps:
  - name: mvn_install
    enable: true
    failure: true
    plugin:
      name: mvn_install
      inputs:
```
